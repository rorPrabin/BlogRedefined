<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>
<% i = 0 %>
<ul class="users">
  <% @users.each do |user| %>
  
  <li>
  <%= i = i+1 %>.
  <%#= gravatar_for user, :size => 50 %>
  <%= link_to user.name, user %>
</li>
  <% end %>
</ul>

<%= will_paginate %>